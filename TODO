- index.mako should really just contain enough information to show the loading screen. Then an AJAX call could actually get the data from Twitter.
- Move OAuth credentials in to a config file
- Webserver could cache information so Twitter API doesn't have to get hit all the time.
